<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AccountManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorSurface">@color/white</item>
        <item name="android:statusBarColor">@color/status_bas_color</item>

    </style>

    <style name="Theme.AccountManager" parent="Base.Theme.AccountManager" />
</resources>